# This directory stores SSL certificates for database connections.
# Download your Aiven CA certificate and save it here as ca.pem.
# Certificate files (.pem, .crt, .key) are gitignored for security.
